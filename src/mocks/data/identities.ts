import { Identity } from "src/utils/api/identities";

const mockIdentities: Identity[] = [
  { id: 1, idpClientID: "identity1" },
  { id: 2, idpClientID: "identity2" },
];

export default mockIdentities;
